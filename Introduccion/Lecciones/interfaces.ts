(() => {
    //Sirven para definir reglas en objetos
    interface Xmen {
        nombre: string;
        edad: number;
        poder?: string;
    }


    // const enviarMision = ( xmen: {nombre:string} ) => {

    //     console.log(`Enviando a ${ xmen.nombre } a la misión`);

    // }

    const wolverine: Xmen = {
        nombre: 'Logan',
        edad: 60
    }

    // enviarMision( wolverine );


})();



